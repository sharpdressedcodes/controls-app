const config = {
    app: {
        port: 3000
    },
    server: {
        port: 3001
    },
    endpoints: {
        api: {
            getControls: '/api/controls/get'
        }
    }
};

module.exports = config;
